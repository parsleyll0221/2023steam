package kr.co.publicvoid.mapper;

import java.util.List;

import kr.co.publicvoid.vo.MemberImageVO;

public interface MemberImageMapper {
	// 등록
	int insert(MemberImageVO memberImageVO);
	
	// 수정
	int update(MemberImageVO memberImageVO);
	
	// 삭제
	int delete(Long memberImageNo);
	
	// 목록 조회
	List<MemberImageVO> selectList();
	
	// 단일 조회
	MemberImageVO selectOne(Long memberImageNo);
	
	// 회원 번호로 단일 조회
	MemberImageVO selectOneByMemberNo(Long memberNo);
}
